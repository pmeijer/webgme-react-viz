/*globals VISUALIZER_INSTANCE_ID*/
import React from 'react';
import ReactDOM from 'react-dom';

import ReactViz from './ReactViz';

ReactDOM.render(<ReactViz />, document.getElementById(VISUALIZER_INSTANCE_ID));
